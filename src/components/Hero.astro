---
---

<section class="pt-16 pb-24 max-sm:pt-10 max-sm:pb-16">
  <div class="container">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">

      <!-- Left: Text -->
      <div>
        <div class="flex items-center gap-2 text-primary text-[11px] font-semibold uppercase
                    tracking-[0.12em] mb-6">
          <span class="material-symbols-outlined text-[16px]">terminal</span>
          <span class="font-mono">[SESSION_INITIATED]</span>
        </div>

        <h1 class="text-[clamp(32px,5vw,52px)] font-bold leading-[1.05] tracking-[-0.04em]
                   uppercase mb-6">
          <span class="text-primary">&gt;</span>
          whoami: I BUILD THE SYSTEMS THAT LET YOUR TEAMS SHIP<span
            class="cursor-blink text-primary font-[800] ml-1">|</span>
        </h1>

        <p class="text-text-muted max-w-[520px] text-[16px] leading-[1.65] mb-9">
          Platform &amp; Infrastructure Engineer with 12+ years designing
          cloud-native systems end-to-end. GitOps, service meshes, Kubernetes,
          Kafka — and the AI automation workflows that cut toil.
          I've been on both sides of the platform.
        </p>

        <div class="flex flex-wrap gap-4">
          <a href="mailto:husamuddin.hamad@gmail.com"
             class="inline-flex items-center justify-center px-8 py-[14px] text-[13px] font-bold
                    uppercase tracking-[0.08em] transition-all duration-100
                    bg-primary text-[#003824] border border-primary
                    hover:bg-[#003824] hover:text-primary">
            INITIALIZE_CONTACT
          </a>
          <a href="https://github.com/husamuddin" target="_blank" rel="noopener"
             class="inline-flex items-center justify-center px-8 py-[14px] text-[13px] font-bold
                    uppercase tracking-[0.08em] transition-all duration-100
                    bg-transparent text-primary border border-outline
                    hover:bg-[var(--primary-dim)] hover:border-primary">
            VIEW_REPOS
          </a>
        </div>
      </div>

      <!-- Right: ASCII Diagram -->
      <div class="bg-bg-low border border-[var(--border-dim)] p-6 font-mono text-[11px]
                  text-[rgba(78,222,163,0.8)] overflow-hidden relative max-sm:text-[9px]">
        <div class="absolute top-0 right-0 px-2 py-1 text-[9px] uppercase tracking-[0.08em]
                    text-outline bg-bg-high">
          Live_Deploy_Visualizer
        </div>
        <pre class="whitespace-pre leading-[1.4] overflow-x-auto text-[10.5px] max-sm:text-[9px]">
[DEPLOYING] &gt;&gt; k8s_cluster_prod
[FETCHING]  &gt;&gt; argocd/applicationset

    +------------------+     +------------------+
    |   Netbird VPN    |&lt;----| External Traffic |
    +--------+---------+     +------------------+
             |
    +--------v---------+     +------------------+
    |   Istio Ingress  |----&gt;|   Dex OIDC SSO   |
    +--------+---------+     +------------------+
             |
    +--------v----------------------------------+
    |           k8s CLUSTER                     |
    |                                           |
    |  +----------+    +------------------+     |
    |  |  ArgoCD  |    |   Apps Workload  |     |
    |  +----------+    +------------------+     |
    |                                           |
    |  +------------------+                     |    +------------+
    |  | External Secrets |-- secrets sync -----|---&gt;| Bitwarden  |
    |  +------------------+                     |    +------------+
    +-------------------------------------------+
             |
    +--------v---------+
    |   PostgreSQL     |
    +------------------+

[STATUS] &gt;&gt; GitOps: SYNCED
[STATUS] &gt;&gt; Health_Check: PASSING
        </pre>
      </div>

    </div>
  </div>
</section>
